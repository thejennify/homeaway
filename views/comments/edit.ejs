<%- include("../partials/header") %>

<div class="container">
    <div class="exit">
        <a href="/listings/<%= homeaway_id%>/"><i class="fas fa-times"></i> close</a>
    </div>
    <div class="ui container segment">
        <form class="ui form" action="/listings/<%= homeaway_id%>/comments/<%= comment._id%>?_method=PUT" method="POST">
            <div class="field">
                <label> Edit Review</label>
                <textarea name="comment[text]"><%= comment.text%></textarea>
            </div>
            <button class="ui button small-btn" type="submit">Post</button>
        </form>
    </div>
</div>